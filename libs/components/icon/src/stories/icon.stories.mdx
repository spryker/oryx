import { Story, Meta, Canvas, ArgsTable } from '@storybook/addon-docs';
import { html } from 'lit';
import { iconTypes } from './model';
import '../index';

# Icon web-component

<Meta title="Icon" />

export const Template = ({ type, size, color }) => {
  return html`
    <div style="color: ${color}">
      <oryx-icon type=${type} size=${size}></oryx-icon>
    </div>
  `;
};

<Canvas>
  <Story
    name="Simple icon"
    argTypes={{
      type: {
        options: iconTypes,
        control: { type: 'select' },
        defaultValue: 'close',
      },
      size: {
        options: ['large', 'medium', 'small'],
        control: { type: 'radio' },
      },
      color: {
        control: { type: 'color' },
        defaultValue: 'red',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Simple icon" />
