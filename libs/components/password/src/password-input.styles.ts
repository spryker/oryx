import { css } from 'lit';

export const passwordInputStyles = css`
  button {
    border: none;
    padding: 0;
    background-color: transparent;
    color: inherit;
    cursor: pointer;
    padding-inline-end: 10px;
  }
`;
