import { ContentComponentSchema } from '@spryker-oryx/experience';
import { MerchantOfferComponent } from './offer.component';

export const merchantOfferSchema: ContentComponentSchema<MerchantOfferComponent> =
  {
    name: 'Offer',
    group: 'Merchant',
  };
