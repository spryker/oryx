import { ContentComponentSchema } from '@spryker-oryx/experience';
import { SiteModeSelectorComponent } from './mode-selector.component';

export const siteModeSelectorSchema: ContentComponentSchema<SiteModeSelectorComponent> =
  {
    name: 'Color mode selector',
    group: 'Site',
  };
