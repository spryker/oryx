import { ContentComponentSchema } from '@spryker-oryx/experience';
import { SiteBreadcrumbsComponent } from './breadcrumbs.component';

export const siteBreadcrumbsSchema: ContentComponentSchema<SiteBreadcrumbsComponent> =
  {
    name: 'Breadcrumbs',
    group: 'Site',
  };
