import { ContentComponentSchema } from '@spryker-oryx/experience';
import { SiteTotalsComponent } from './totals.component';

export const siteTotalsSchema: ContentComponentSchema<SiteTotalsComponent> = {
  name: 'Site Totals',
  group: 'Site',
};
