export interface CartSummaryOptions {
  /**
   * The maximum number of items to display in the summary. When the number of items exceeds
   * this value, the mark will display the maximum number of items followed by a plus sign.
   */
  maxItems?: number;
}
