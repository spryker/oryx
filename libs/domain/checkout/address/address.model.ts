export interface CheckoutAddressOptions {
  /**
   * Indicates whether the available addresses - if any - are listed so that the user
   * can select the active one. If this value is `false`, the active address is shown.
   */
  enableList?: boolean;
}
