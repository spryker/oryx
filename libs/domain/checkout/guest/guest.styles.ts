import { HeadingTag, headingUtil } from '@spryker-oryx/ui/heading';
import { css } from 'lit';

export const checkoutGuestStyles = css`
  p.have-an-account {
    ${headingUtil(HeadingTag.H5)};
    margin-inline: auto 10px;
  }
`;
