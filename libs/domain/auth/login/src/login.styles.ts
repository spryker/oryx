import { css } from 'lit';

export const styles = css`
  form {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .options {
    display: flex;
    align-items: center;
  }

  .options .forgot-password {
    margin-inline-start: auto;
  }
`;
