import { ContentComponentSchema } from '@spryker-oryx/experience';
import { ListsComponent } from './lists.component';

export const listsComponentSchema: ContentComponentSchema<ListsComponent> = {
  name: 'Lists',
  group: 'Picking',
};
