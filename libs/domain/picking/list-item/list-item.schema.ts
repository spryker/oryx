import { ContentComponentSchema } from '@spryker-oryx/experience';
import { ListItemComponent } from './list-item.component';

export const listItemComponentSchema: ContentComponentSchema<ListItemComponent> =
  {
    name: 'List Item',
    group: 'Picking',
  };
