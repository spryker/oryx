import { css } from 'lit';

export const filtersComponentStyles = css`
  oryx-heading {
    flex-grow: 1;
    text-align: center;
    font-weight: 600;
  }

  [slot='footer'] {
    flex-grow: 1;
  }
`;
