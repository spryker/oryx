import { css } from 'lit';

export const pickingSearchComponentStyles = css`
  oryx-search {
    --floating-padding-start: 16px;
    --floating-width: calc(var(--oryx-layout-container-width) - 32px);
  }
`;
