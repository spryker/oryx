export enum Size {
  large = 'large',
  medium = 'medium',
  small = 'small',
}

export enum Position {
  START = 'start',
  END = 'end',
}

export enum Direction {
  horizontal = 'horizontal',
  vertical = 'vertical',
}
