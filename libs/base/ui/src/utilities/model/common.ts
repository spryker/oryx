export enum Size {
  large = 'large',
  medium = 'medium',
  small = 'small',
}

export const enum AlertType {
  Info = 'info',
  Success = 'success',
  Warning = 'warning',
  Error = 'error',
}

export enum Position {
  START = 'start',
  END = 'end',
}

export enum Direction {
  horizontal = 'horizontal',
  vertical = 'vertical',
}
