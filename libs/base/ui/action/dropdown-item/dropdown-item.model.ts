export interface DropdownItemComponentAttributes {
  /**
   * The icon displayed in the dropdown. It can be an `Icons` enum value or a string representing the icon.
   */
  icon?: string;

  /**
   * The URL to link to when the dropdown is clicked.
   */
  url?: string;
  /**
   * The text displayed in the dropdown.
   */
  text?: string;
}
