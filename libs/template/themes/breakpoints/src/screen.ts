import { ThemeDefaultMedia } from '@spryker-oryx/core';
import { Size } from '@spryker-oryx/utilities';

export const smScreen = {
  [ThemeDefaultMedia.Screen]: Size.Sm,
};

export const mdScreen = {
  [ThemeDefaultMedia.Screen]: Size.Md,
};

export const lgScreen = {
  [ThemeDefaultMedia.Screen]: Size.Lg,
};
