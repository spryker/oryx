import { css } from 'lit';

export const labelStyles = css`
  label {
    display: flex;
    margin-bottom: 8px;
    cursor: inherit;
    font-weight: 400;
    font-size: 12px;
    text-transform: uppercase;
  }

  sup {
    margin-inline-start: 4px;
  }
`;
