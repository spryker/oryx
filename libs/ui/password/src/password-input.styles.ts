import { css } from 'lit';

export const passwordInputStyles = css`
  button {
    border: none;
    padding: 0;
    background-color: transparent;
    color: inherit;
    cursor: pointer;
    width: 24px;
    height: 24px;
  }
`;
