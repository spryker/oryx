import { Size } from '@spryker-oryx/ui/utilities';

export interface IconButtonProperties {
  /**
   * Indicates the icon size of the icon button component, `small`, `medium` or `large`.
   *
   * @default `Size.medium`
   */
  size: Size;
}
