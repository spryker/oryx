import { css } from 'lit';
import { LayoutStyles } from '../../layout.model';

export const styles: LayoutStyles = {
  styles: css`
    :host {
      position: sticky;
      inset-block-start: 0;
    }
  `,
};
