import { css } from 'lit';

export const styles = css`
  oryx-button {
    --oryx-button-color: var(--oryx-color-neutral-12);
    --oryx-icon-size: 24px;

    justify-content: flex-start;
    padding: 9px 15px;
  }
`;
