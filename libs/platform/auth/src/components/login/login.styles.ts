import { css } from 'lit';

export const styles = css`
  .login-form {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .options {
    display: flex;
    align-items: center;
  }

  .options oryx-link {
    margin-inline-start: auto;
  }
`;
