export interface ScreenSize {
  /**
   * Indicates the minimum screen size.
   *
   * The minimum screen size is used to build a CSS media query.
   */
  min?: number;
  /**
   * Indicates the maximum screen size.
   *
   * The maximum screen size is used to build a CSS media query.
   */
  max?: number;
}
