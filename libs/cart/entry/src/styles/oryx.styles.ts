import { css } from 'lit';

export const styles = css`
  :host {
    border-radius: var(--oryx-border-radius-small);
    border: 2px solid var(--oryx-color-neutral-greyblue);
    background: var(--oryx-color-canvas);
  }
`;
