{
  "projectType": "application",
  "root": "apps/storybook",
  "sourceRoot": "apps/storybook",
  "prefix": "fes",
  "targets": {
    "serve": {
      "executor": "@nrwl/storybook:storybook",
      "options": {
        "uiFramework": "@storybook/web-components",
        "port": 4400,
        "config": {
          "configFolder": "apps/storybook/.storybook"
        },
        "staticDir": ["libs/ui/public"]
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "build": {
      "executor": "@nrwl/storybook:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "uiFramework": "@storybook/web-components",
        "config": {
          "configFolder": "apps/storybook/.storybook"
        },
        "outputPath": "dist/storybook/all",
        "staticDir": ["libs/ui/public"]
      }
    },
    "generate-dep-tree": {
      "executor": "./tools/executors/generate-dependency-tree:generate-dep-tree",
      "options": {
        "project": "storybook"
      }
    },
    "check-dep-tree": {
      "executor": "./tools/executors/check-dependency-tree:check-dep-tree",
      "options": {
        "project": "storybook"
      }
    },
    "visual-regression-pr": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "chromatic --ci --exit-zero-on-changes"
      }
    },
    "visual-regression-branch": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "chromatic --ci --exit-zero-on-changes --auto-accept-changes"
      }
    }
  },
  "tags": [],
  "implicitDependencies": ["ui", "content"]
}
