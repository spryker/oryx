{
  "name": "fes-storefront",
  "version": "1.0.0-alpha.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite -c vite.config.dev.ts",
    "preview": "vite preview",
    "build": "vite build",
    "dev:ssr": "node --experimental-vm-modules server/server",
    "build:server": "vite build -c vite.config.server.ts",
    "build:ssr": "npm run build && npm run build:server"
  },
  "peerDependencies": {
    "@spryker-oryx/core": "*",
    "@spryker-oryx/presets": "*",
    "@spryker-oryx/themes": "*",
    "@spryker-oryx/storefront": "*"
  },
  "devDependencies": {
    "vite": "^3.0.9"
  }
}
