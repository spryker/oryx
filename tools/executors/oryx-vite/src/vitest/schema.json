{
  "type": "object",
  "cli": "nx",
  "properties": {
    "watch": {
      "type": "boolean",
      "description": "Set watch mode",
      "default": false
    },
    "coverageReportDist": {
      "type": "string",
      "description": "Set coverage report dist",
      "default": "../../coverage"
    }
  },
  "description": "Extends default vitest UserConfig interface"
}
