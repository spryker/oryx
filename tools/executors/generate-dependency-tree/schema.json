{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "project": {
      "type": "string",
      "description": "Project which to generate the dependencies for"
    }
  }
}
