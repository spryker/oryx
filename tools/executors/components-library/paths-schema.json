{
  "title": "TsConfig Paths Generator",
  "description": "Generates paths based on library scaffolding and package.json",
  "type": "object",
  "cli": "nx",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name tsConfig file.",
      "default": "tsconfig.base.json"
    },
    "update": {
      "type": "boolean",
      "description": "Flag for updating mode.",
      "default": false
    },
    "exclude": {
      "type": "array",
      "description": "List of folders which will be excluded of paths generations."
    }
  }
}
